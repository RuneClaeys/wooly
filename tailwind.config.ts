import type { Config } from 'tailwindcss';

export default {
   content: [],
   theme: {
      extend: {
         fontFamily: {
            sans: ['Poppins', 'sans-serif'],
         },
      },
   },
   plugins: [],
} satisfies Config;
